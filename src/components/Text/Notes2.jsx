const react = require("react");

function Notes2() {
  return (
    <div className="note">
      <h1>ABOUT REACT.JS</h1>
      <p>
        React.js is created by facebook which is a front end library for
        Javascript. It uses less code to build interactive user interface and
        web application than with vanilla javascript which is the orignal
        version of javascript.
      </p>
    </div>
  );
}

export default Notes2;
